# Глоссарий Backend-Driven UI

Веб-приложение для управления глоссарием терминов по теме **"Исследование Backend-Driven UI для быстрой доставки изменений интерфейса конечному пользователю"**.

## 📋 Описание

Проект представляет собой полнофункциональное REST API на FastAPI с веб-интерфейсом для визуализации семантического графа терминов. Приложение поддерживает все CRUD операции для управления терминами и автоматически создает связи между ними для построения семантического графа.

## ✨ Основные возможности

- ✅ Получение списка всех терминов с пагинацией
- ✅ Поиск термина по ключевому слову
- ✅ Добавление нового термина с описанием и источником
- ✅ Обновление существующего термина
- ✅ Удаление термина из глоссария
- ✅ Валидация данных с помощью Pydantic
- ✅ Автоматическая генерация OpenAPI документации
- ✅ SQLite база данных с автоматическими миграциями
- ✅ Визуализация семантического графа терминов (MindMap)
- ✅ Docker контейнеризация для быстрого развертывания

## 🛠 Технологический стек

- **Backend**: FastAPI, SQLAlchemy, Pydantic
- **База данных**: SQLite
- **Frontend**: HTML, CSS, JavaScript (vis-network для визуализации графа)
- **Контейнеризация**: Docker, Docker Compose
- **Документация**: Автоматическая OpenAPI (Swagger UI)

## 📦 Установка и запуск

### Вариант 1: Запуск с Docker Compose (рекомендуется)

#### Требования
- Docker
- Docker Compose

#### Шаги запуска

1. **Убедитесь, что Docker запущен**:
```bash
docker --version
docker compose version
```

Если Docker не запущен, запустите Docker Desktop.

2. **Запустите приложение**:
```bash
docker compose up --build
```

Или в фоновом режиме:
```bash
docker compose up --build -d
```

3. **Откройте в браузере**:
   - **Frontend (граф и список)**: http://localhost:8000/static/index.html
   - **API и документация**: http://localhost:8000/docs
   - **API напрямую**: http://localhost:8000

4. **Остановка приложения**:
```bash
docker compose down
```

**Примечание**: Если у вас установлен Docker Desktop или Docker версии 20.10+, используйте команду `docker compose` (без дефиса). Для старых версий Docker используйте `docker-compose`.

### Вариант 2: Локальный запуск (без Docker)

#### Требования
- Python 3.11 или выше
- pip

#### Шаги установки

1. **Создание виртуального окружения**:
```bash
python -m venv env
```

2. **Активация виртуального окружения**:
   - На macOS/Linux:
   ```bash
   source env/bin/activate
   ```
   - На Windows:
   ```bash
   env\Scripts\activate
   ```

3. **Установка зависимостей**:
```bash
pip install -r requirements.txt
```

4. **Запуск приложения**:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

5. **Откройте в браузере**:
   - **Frontend (граф и список)**: http://localhost:8000/static/index.html
   - **API и документация**: http://localhost:8000/docs
   - **API напрямую**: http://localhost:8000

6. **Остановка приложения**: Нажмите `Ctrl+C` в терминале

### Проверка работы

#### Проверить, что API работает:
```bash
curl http://localhost:8000/
```

#### Проверить список терминов:
```bash
curl http://localhost:8000/terms
```

#### Проверить статус контейнера (если используете Docker):
```bash
docker compose ps
```

#### Посмотреть логи (если используете Docker):
```bash
docker compose logs -f
```

### Полезные команды

#### Перезапуск приложения (Docker):
```bash
docker compose restart
```

#### Пересборка и запуск (Docker):
```bash
docker compose up --build -d
```

#### Очистка (Docker):
```bash
docker compose down -v  # удалит также volumes
```

### Решение проблем

#### Порт 8000 занят
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # вместо 8000:8000
```

Или при локальном запуске:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8001
```

#### База данных не создается
Убедитесь, что папка `data/` существует и доступна для записи:
```bash
mkdir -p data
chmod 755 data
```

#### Ошибки при сборке Docker
```bash
docker compose down
docker compose build --no-cache
docker compose up
```

## 📚 API Endpoints

### Основные операции

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Информация об API |
| GET | `/terms` | Получить список всех терминов |
| GET | `/terms/{term_id}` | Получить термин по ID |
| GET | `/terms/search?keyword={keyword}` | Поиск термина по ключевому слову |
| POST | `/terms` | Добавить новый термин |
| PUT | `/terms/{term_id}` | Обновить существующий термин |
| DELETE | `/terms/{term_id}` | Удалить термин |
| GET | `/terms/graph/data` | Получить данные для визуализации графа |

### Примеры использования API

#### Получить все термины
```bash
curl http://localhost:8000/terms
```

#### Поиск термина
```bash
curl "http://localhost:8000/terms/search?keyword=Backend-Driven"
```

#### Добавить новый термин
```bash
curl -X POST "http://localhost:8000/terms" \
  -H "Content-Type: application/json" \
  -d '{
    "keyword": "REST API",
    "description": "Архитектурный стиль для разработки веб-сервисов",
    "source": "Web Development Standards"
  }'
```

#### Обновить термин
```bash
curl -X PUT "http://localhost:8000/terms/1" \
  -H "Content-Type: application/json" \
  -d '{
    "description": "Обновленное описание термина"
  }'
```

#### Удалить термин
```bash
curl -X DELETE "http://localhost:8000/terms/1"
```

## 🗄 Структура базы данных

### Таблица `terms`
- `id` (Integer, Primary Key) - Уникальный идентификатор
- `keyword` (String, Unique) - Ключевое слово термина
- `description` (Text) - Описание термина
- `source` (String, Optional) - Источник определения
- `created_at` (DateTime) - Дата создания
- `updated_at` (DateTime) - Дата последнего обновления

### Таблица `term_relations`
- `id` (Integer, Primary Key) - Уникальный идентификатор
- `term_id` (Integer, Foreign Key) - ID термина
- `related_term_id` (Integer, Foreign Key) - ID связанного термина
- `relation_type` (String) - Тип связи (related, synonym, antonym)

## 🎨 Визуализация семантического графа

Веб-интерфейс для визуализации терминов и их взаимосвязей доступен по адресу `/static/index.html`.

### Возможности интерфейса:
- Интерактивный граф с возможностью масштабирования и перемещения
- Поиск терминов по ключевому слову
- Подсветка связанных терминов при клике на узел
- Список всех терминов с описаниями
- Статистика по количеству терминов и связей

## 🐳 Контейнеризация

### Выбор формата контейнера

Проект использует **Docker** как стандарт де-факто для контейнеризации приложений. Выбор обоснован следующими факторами:

1. **Универсальность**: Docker поддерживается всеми основными облачными платформами (AWS, Azure, GCP, Yandex Cloud)
2. **Отечественные решения**: 
   - **Yandex Cloud** - полная поддержка Docker контейнеров
   - **Selectel** - поддержка Docker и Kubernetes
   - **VK Cloud** - поддержка Docker контейнеров
   - **Ростелеком Cloud** - поддержка контейнерных технологий
3. **Простота развертывания**: Docker Compose позволяет быстро развернуть приложение одной командой
4. **Портативность**: Образ можно запустить на любой платформе, поддерживающей Docker

### Альтернативные форматы

- **OCI (Open Container Initiative)** - стандарт, совместимый с Docker
- **Podman** - альтернатива Docker с совместимым форматом образов
- **Kubernetes** - для оркестрации множества контейнеров (может использовать Docker образы)

## 📝 Импорт глоссария

### Автоматический импорт из list.txt

В проекте есть скрипт `import_glossary.py` для импорта терминов из файла `list.txt` (формат RDF/триплетов).

**Импорт уже выполнен**: База данных содержит **185 терминов** и **247 связей** из вашей онтологии.

Для повторного импорта:

```bash
# Внутри контейнера
docker compose exec glossary-api python import_glossary.py

# Или локально (требуется виртуальное окружение)
python import_glossary.py
```

### Начальные данные (резервные)

Если база данных пуста, при первом запуске приложение автоматически создает базу данных и заполняет ее начальными терминами по теме Backend-Driven UI:

- Backend-Driven UI
- Server-Driven UI
- JSON Schema
- Over-the-Air Update
- Feature Flag
- A/B Testing
- Dynamic UI
- API Gateway
- Content Delivery Network
- Hot Reload

## 🔧 Конфигурация

### Переменные окружения

При необходимости можно настроить следующие параметры через переменные окружения:

- `DATABASE_URL` - URL базы данных (по умолчанию: `sqlite:///./glossary.db`)
- `HOST` - Хост для запуска сервера (по умолчанию: `0.0.0.0`)
- `PORT` - Порт для запуска сервера (по умолчанию: `8000`)

## 📖 Документация API

Автоматически сгенерированная документация доступна по следующим адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

## 🚀 Развертывание на публичном сервере

### Развертывание на Heroku

1. Создайте файл `Procfile`:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

2. Установите Heroku CLI и выполните:
```bash
heroku create your-app-name
git push heroku main
```

### Развертывание на Yandex Cloud

1. Создайте Docker образ и загрузите в Container Registry
2. Создайте сервисный аккаунт
3. Создайте серверную группу с вашим образом
4. Настройте балансировщик нагрузки

### Развертывание на обычном VPS

1. Установите Docker на сервер:
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
```

2. Клонируйте репозиторий на сервер
3. Запустите с помощью docker compose:
```bash
docker compose up -d
```

4. Настройте Nginx как reverse proxy (опционально)

## 📸 Скриншоты

### Swagger UI (API документация)
![Swagger UI](screenshots/swagger.png)

### Визуализация семантического графа
![Graph Visualization](screenshots/graph.png)

### Список терминов
![Terms List](screenshots/terms.png)

*Примечание: Добавьте скриншоты вашего развернутого приложения в папку `screenshots/`*

## 🧪 Тестирование

Для тестирования API можно использовать:

1. **Swagger UI** - интерактивная документация с возможностью выполнения запросов
2. **curl** - командная строка
3. **Postman** - графический интерфейс для тестирования API
4. **HTTPie** - удобная альтернатива curl

## 📄 Лицензия

Этот проект создан в рамках учебной практики.

## 👤 Автор

Создано для научной работы по теме "Исследование Backend-Driven UI для быстрой доставки изменений интерфейса конечному пользователю"

## 🔗 Полезные ссылки

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [Pydantic Documentation](https://docs.pydantic.dev/)
- [Docker Documentation](https://docs.docker.com/)
- [vis-network Documentation](https://visjs.github.io/vis-network/docs/network/)

---

**Дата создания**: 2024
**Версия**: 1.0.0
