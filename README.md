# Глоссарий Backend-Driven UI

Веб-приложение для управления глоссарием терминов по теме "Исследование Backend-Driven UI для быстрой доставки изменений интерфейса конечному пользователю".

## 🚀 Возможности

- **CRUD операции** для терминов:
  - Получение списка всех терминов
  - Поиск термина по ключевому слову
  - Добавление нового термина
  - Обновление существующего термина
  - Удаление термина

- **Управление связями** между терминами для построения семантического графа

- **Интерактивная визуализация** графа терминов с использованием D3.js

- **Автоматическая документация API** через OpenAPI/Swagger

- **Docker контейнеризация** для быстрого развертывания

## 📋 Технологический стек

- **Backend**: FastAPI (Python 3.11)
- **База данных**: SQLite с автоматическими миграциями
- **Валидация**: Pydantic
- **Фронтенд**: HTML + JavaScript (D3.js для визуализации)
- **Контейнеризация**: Docker + Docker Compose

## 🛠️ Установка и запуск

### Вариант 1: Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yuliya-karimova/python-itmo-glossary.git
cd python-itmo-glossary
```

2. Запустите приложение:
```bash
docker compose up --build
```

3. Откройте в браузере:
   - Фронтенд: http://localhost:8000
   - API документация: http://localhost:8000/docs
   - Альтернативная документация: http://localhost:8000/redoc

### Вариант 2: Локальная установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Импортируйте данные:
```bash
python app/import_data.py
```

3. Запустите сервер:
```bash
uvicorn app.main:app --reload
```

4. Откройте в браузере:
   - Фронтенд: http://localhost:8000
   - API документация: http://localhost:8000/docs

## 📚 API Endpoints

### Термины

- `GET /api/terms` - Получить список всех терминов
- `GET /api/terms/{term_id}` - Получить информацию о термине по ID
- `GET /api/terms/search/{keyword}` - Поиск терминов по ключевому слову
- `POST /api/terms` - Добавить новый термин
- `PUT /api/terms/{term_id}` - Обновить термин
- `DELETE /api/terms/{term_id}` - Удалить термин

### Связи

- `GET /api/links` - Получить все связи
- `POST /api/links` - Создать связь между терминами
- `DELETE /api/links/{link_id}` - Удалить связь

### Граф

- `GET /api/graph` - Получить полный граф для визуализации

## 🐳 Docker

### Обоснование выбора формата контейнера

Выбран **Docker** как наиболее распространенный и поддерживаемый формат контейнеризации:

**Преимущества Docker:**
- ✅ Широкая поддержка на всех платформах (Linux, Windows, macOS)
- ✅ Интеграция с CI/CD системами (GitHub Actions, GitLab CI, Jenkins)
- ✅ Большое сообщество и документация
- ✅ Простота развертывания на облачных платформах (AWS, Azure, GCP, Yandex Cloud)

**Отечественные альтернативы:**
- **Yandex Cloud Container Registry** - полностью поддерживает Docker образы
- **VK Cloud Solutions** - поддерживает Docker контейнеры
- **Selectel** - поддерживает Docker
- **Ростелеком Cloud** - поддерживает Docker

Все основные российские облачные провайдеры поддерживают Docker формат, что делает его универсальным выбором для развертывания.

### Сборка образа

```bash
docker build -t python-itmo-glossary .
```

### Запуск контейнера

```bash
docker run -p 8000:8000 python-itmo-glossary
```

## 📊 Структура проекта

```
python-itmo-glossary/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение
│   ├── models.py            # SQLAlchemy модели
│   ├── schemas.py           # Pydantic схемы
│   ├── import_data.py       # Скрипт импорта из CSV
│   └── templates/
│       └── index.html       # Фронтенд с визуализацией
├── data/                    # Директория для БД
├── screenshots/             # Скриншоты приложения
├── static/                  # Статические файлы
├── terms.csv                # Исходные данные терминов
├── links.csv                # Исходные данные связей
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## 🔍 Использование API

### Примеры запросов

#### Получить все термины
```bash
curl http://localhost:8000/api/terms
```

#### Добавить новый термин
```bash
curl -X POST http://localhost:8000/api/terms \
  -H "Content-Type: application/json" \
  -d '{"term": "Новый термин", "definition": "Описание термина"}'
```

#### Поиск терминов
```bash
curl http://localhost:8000/api/terms/search/Backend
```

#### Получить граф
```bash
curl http://localhost:8000/api/graph
```

## 🌐 Развертывание на публичном сервере

### Render.com (рекомендуется - простой и бесплатный)

1. Зарегистрируйтесь на [render.com](https://render.com) через GitHub
2. Создайте новый **Web Service**
3. Подключите ваш GitHub репозиторий
4. Настройки (автоматически применяются через `render.yaml`):
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
5. Выберите **Free** план
6. Нажмите **Create Web Service**

Подробные инструкции: см. `RENDER_DEPLOY.md`

### Другие варианты:

1. **Heroku**:
   - Добавьте `Procfile` с командой: `web: uvicorn app.main:app --host 0.0.0.0 --port $PORT`
   - Задеплойте через Git

2. **Yandex Cloud**:
   - Создайте Container Registry
   - Загрузите образ: `docker push cr.yandex/<registry-id>/python-itmo-glossary`
   - Создайте сервис в Yandex Cloud Run

3. **VPS сервер**:
   - Установите Docker на сервере
   - Склонируйте репозиторий
   - Запустите: `docker-compose up -d`

## 📸 Скриншоты

Скриншоты развернутого приложения находятся в директории `screenshots/`.

## 👤 Автор

Создано для научной работы "Исследование Backend-Driven UI для быстрой доставки изменений интерфейса конечному пользователю"

