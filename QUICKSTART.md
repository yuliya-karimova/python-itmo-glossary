# Быстрый старт

## 🚀 Запуск с Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий (если нужно)
git clone <repository-url>
cd python-itmo-glossary

# Запустите приложение
docker compose up --build

# Примечание: Если команда не работает, попробуйте:
# docker-compose up --build  (для старых версий Docker)

# Приложение будет доступно по адресу:
# - API: http://localhost:8000
# - Swagger: http://localhost:8000/docs
# - Frontend: http://localhost:8000/static/index.html
```

## 🐍 Локальный запуск

```bash
# Создайте виртуальное окружение
python -m venv env
source env/bin/activate  # На Windows: env\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
uvicorn main:app --reload

# Приложение будет доступно по адресу: http://localhost:8000
```

## 📝 Первые шаги

1. Откройте Swagger UI: http://localhost:8000/docs
2. Попробуйте получить список терминов: GET `/terms`
3. Откройте визуализацию графа: http://localhost:8000/static/index.html
4. Добавьте новый термин через Swagger UI или curl

## 🔍 Примеры запросов

### Получить все термины
```bash
curl http://localhost:8000/terms
```

### Добавить термин
```bash
curl -X POST "http://localhost:8000/terms" \
  -H "Content-Type: application/json" \
  -d '{
    "keyword": "Новый термин",
    "description": "Описание термина",
    "source": "Источник"
  }'
```

### Поиск термина
```bash
curl "http://localhost:8000/terms/search?keyword=Backend"
```

## 📚 Дополнительная информация

Подробная документация доступна в файле [README.md](README.md)

