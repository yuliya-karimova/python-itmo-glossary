# Развертывание на Render.com

## 🚀 Быстрый старт

### Шаг 1: Зарегистрируйтесь на Render.com

1. Перейдите на [render.com](https://render.com)
2. Нажмите "Get Started" и войдите через GitHub
3. Предоставьте доступ к вашему репозиторию

### Шаг 2: Создайте новый Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите ваш GitHub репозиторий `python-itmo-glossary`
3. Нажмите **"Connect"**

### Шаг 3: Настройки (если используете render.yaml - настройки применятся автоматически)

Если вы используете файл `render.yaml`, Render автоматически применит эти настройки. Если нет, заполните вручную:

**Основные настройки:**
- **Name**: `python-itmo-glossary` (или любое имя по желанию)
- **Environment**: `Python 3`
- **Region**: Выберите ближайший регион (например, Frankfurt для России)

**Build & Deploy:**
- **Build Command**: 
  ```bash
  pip install -r requirements.txt
  ```
- **Start Command**: 
  ```bash
  uvicorn app.main:app --host 0.0.0.0 --port $PORT
  ```

**Environment Variables** (опционально):
- `PYTHON_VERSION` = `3.11`
- `DB_PATH` = `./data/glossary.db`

**Plan:**
- Выберите **Free** план (бесплатно, с ограничениями)

### Шаг 4: Deploy

1. Нажмите **"Create Web Service"**
2. Render автоматически начнет сборку и деплой
3. Дождитесь завершения деплоя (обычно 2-5 минут)

### Шаг 5: Получите URL

После успешного деплоя ваше приложение будет доступно по адресу:

- **Фронтенд**: `https://python-itmo-glossary.onrender.com` (или ваше имя)
- **API**: `https://python-itmo-glossary.onrender.com/api/terms`
- **Документация**: `https://python-itmo-glossary.onrender.com/docs`

---

## 📝 Важные заметки

### Автоматический импорт данных

Данные из CSV файлов (`terms.csv`, `links.csv`) автоматически импортируются при первом запуске приложения (если база пустая), благодаря событию `startup` в `app/main.py`.

### База данных

**Важно**: На бесплатном плане Render:
- Данные SQLite хранятся в эфемерной файловой системе
- При перезапуске сервиса данные могут сброситься
- Для постоянного хранения рекомендуется использовать **PostgreSQL** (бесплатно на Render)

**Использование PostgreSQL** (опционально):
1. Создайте PostgreSQL базу в Render Dashboard
2. Получите `DATABASE_URL`
3. Обновите `app/models.py` для использования PostgreSQL вместо SQLite

### Автоматический деплой

Render автоматически передеплоит приложение при пуше в ветку `main` (или указанную ветку) на GitHub.

### Health Check

Render использует `/api/terms` как health check endpoint для проверки работоспособности приложения.

---

## 🔍 Проверка работы

После деплоя проверьте:

1. **Фронтенд**: Откройте главную страницу - должна загрузиться визуализация графа
2. **API**: `https://your-app.onrender.com/api/terms` - должен вернуть список терминов
3. **Документация**: `https://your-app.onrender.com/docs` - должна открыться Swagger UI

---

## 🆘 Troubleshooting

### Приложение не запускается

Проверьте логи в Render Dashboard:
- **Events** → **Build Log** - ошибки сборки
- **Logs** - ошибки во время выполнения

### 502 Bad Gateway

Обычно означает, что приложение не запустилось:
- Проверьте логи на ошибки
- Убедитесь, что `startCommand` правильный
- Проверьте, что порт использует `$PORT` (обязательно для Render)

### Данные не сохраняются

На бесплатном плане SQLite данные могут сброситься при перезапуске. Используйте PostgreSQL для постоянного хранения.

---

## 📚 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Python Guide](https://render.com/docs/deploy-fastapi)
- [Render PostgreSQL](https://render.com/docs/databases)

---

**Удачного деплоя! 🎉**
